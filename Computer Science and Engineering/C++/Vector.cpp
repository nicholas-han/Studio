// Declare empty vector, no elements initially
vector<int> v;

// Declare vector with 10 elements, each initialized to a default value of 0
vector<int> ve(10);

// Declare vector with 15 elements, each initialized to a value of 27
vector<int> vec(15, 27);

// Declare a new vector that is a copy of an already existing vector
vector<int> vect(ve);
